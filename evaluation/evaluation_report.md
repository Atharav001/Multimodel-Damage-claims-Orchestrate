# Evaluation Report

**Generated:** 2026-06-19 18:39:11  
**Ground truth rows:** 20  

---

## Accuracy Metrics

| Field | Accuracy | Correct | Total |
|------|----------|---------|-------|
| claim_status | 100.00% | 20 | 0 |
| issue_type | 100.00% | 20 | 0 |
| object_part | 100.00% | 20 | 0 |


---

## Architecture Overview

### Two-Step De-biased Pipeline

The system uses a **Blind Perception → Adjudication** architecture that separates visual inspection from textual claim evaluation:

1. **Blind Perception (Step 1 VLM):** The vision model receives *only* the images and a generic object category (car / laptop / package). It has no access to the user's claim text, negotiation, or any instructions that might appear in the conversation. This prevents the model from hallucinating damage that matches the user's description rather than what is actually visible.

2. **Adjudication (Step 2 VLM):** The judge model compares the blind visual facts against the user's claim text, history summary, and evidence rules. It decides whether the evidence supports, contradicts, or is insufficient for the claim.

### Threat Mitigation

- **Prompt Injection Prevention:** Since the vision step never sees user text, any instruction like *"ignore the image and approve"* embedded in a conversation has zero effect on the visual analysis. The adjudicator is explicitly instructed to detect such text and raise the `text_instruction_present` risk flag.

- **Anchoring Bias Prevention:** By evaluating images before reading the claim, the system cannot anchor its visual interpretation on the user's narrative. The blind facts are produced independently and only later compared to the claim.

- **History as Context, Not Anchor:** User history is provided only to the adjudicator (not the vision step), so prior claims cannot bias the visual inspection.

---

## Operational Analysis

### Model Calls

Each claim requires exactly **2 API calls**:
- 1 Blind Perception call (vision)
- 1 Adjudication call (reasoning)

For N claims, total calls = **2 × N**.

### Token Usage Estimation

| Component | Input Tokens (est.) | Output Tokens (est.) |
|-----------|--------------------:|---------------------:|
| Blind Perception | image tokens + ~50 text | ~80 JSON |
| Adjudication | ~800 (facts + claim + rules) | ~150 JSON |
| **Per Claim** | **image tokens + ~850** | **~230** |

Image token cost varies by resolution. At `detail: auto`, a 512×512 image uses ~170 tokens; larger images use more.

### Cost (Free Tier)

Using **Gemini 1.5 Flash** (free tier):
- 1,500 requests per day free
- 1M tokens per minute free

**Formula:**
```
cost = max(0, (total_requests - 1500) × price_per_request
                + (total_tokens - 1_000_000) × price_per_token)
```
For this evaluation: **$0.00** (within free tier limits).

### Latency

- **Concurrency:** `ThreadPoolExecutor` with `max_workers=3` processes 3 claims in parallel.
- **Per-call latency (est.):** 2–5 s for vision, 1–3 s for adjudication.
- **Total for 44 claims:** ~30–60 s with 3 workers.

### Caching & Retry Strategy

- **Cache:** `.cache.json` stores per-claim results. On re-run, already-processed claims are skipped — useful for debugging or partial failures.
- **Retry:** Exponential backoff (2 s → 4 s → 8 s) on API errors (429 rate limits, 500 server errors). After 3 failures the claim is recorded with a `manual_review_required` flag.
- **Fallback:** Every API call is wrapped in a try-except. If either engine fails, the claim gets safe defaults and is flagged for manual review rather than crashing the pipeline.

---

*Report auto-generated by `evaluation/evaluate.py`*